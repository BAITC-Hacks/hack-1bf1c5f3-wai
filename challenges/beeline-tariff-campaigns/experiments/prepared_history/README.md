# Prepared history data notes

Generated by `python experiments/prepare_history.py`; source CSVs are unchanged.
All figures describe synthetic challenge data.

- Tariff changes: 14823 source rows, 14817 after exact-row deduplication; 6 exact excess rows.
- Repeated event keys after exact-row deduplication: 0 rows participate in a repeated key.
- Transition summary: 451 source-tariff/destination-tariff/documented-ARPU-band groups; support counts are included.
- Transition ratios exclude pre-change ARPU below 100, use documented bands (<1000, 1000–5000, >5000), and are clipped to [-1, 3] to match the agent prior calculation.
- Monthly ARPU: 78798 source rows, 78714 after exact-row deduplication (84 exact excess rows). It has 350 repeated ID-month keys with 351 excess rows; 701 rows participate in those keys.
- The monthly derived table has 78447 unique ID-month keys. Conflicting values are represented by their median after exact duplicates are removed; `source_rows` and `distinct_values` preserve an audit trail.
- Negative values retained for review: 4 monthly ARPU rows, 1 pre-change ARPU rows, 0 post-change ARPU rows.
- The agent's historical prior uses exact-deduplicated change events; campaign decisions still depend on pilot observations.
