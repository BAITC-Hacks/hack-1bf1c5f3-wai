# Teammate source snapshot

Exact participant Python sources fetched from `origin/main` commit
`e2b864d8c326cfc1311c9a64feab1e38b4406052` on 2026-09-23.

These four modules are experimental comparison inputs, not the active submission.
Run `../compare_main.py` from the challenge directory to load them together against
the unchanged official evaluator and shared participant data. The loader supplies
the challenge directory as the module file location so relative history paths work.

See [comparison findings](../main_comparison.md) and source hashes in
[raw results](../main_comparison_results.json).
